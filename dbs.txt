database database

    collections
    
        users  (users)
            id, name, password, tournament ids
            
        admins (administrators)
            id, name, password
            
        tourns (tournaments)
            id, name, teams, results, admin id 

	teams  (teams)
	    id, name, coach, user ids
